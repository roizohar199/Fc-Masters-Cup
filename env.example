# ====================================
# FC Masters Cup - Environment Variables
# ====================================
# העתק קובץ זה ל-.env ומלא ערכים

# ====================================
# הגדרות בסיסיות
# ====================================
PORT=8787
DB_PATH=./server/tournaments.sqlite
TZ=Asia/Jerusalem
NODE_ENV=production

# ====================================
# אבטחה ותוקנים (חובה!)
# ====================================
# JWT_SECRET - מפתח סודי להצפנת טוקנים (חובה לשנות!)
# צור מפתח חזק בפרודקשן: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-CHANGE-THIS-TO-RANDOM-STRING

# פרטי אדמין ראשוני (יווצר אוטומטית בהרצה ראשונה)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=YourStrongPassword123!

# רשימת כתובות מייל מנהלים (לקבלת התראות) - הפרד בפסיקים
# אם יש מנהל אחד: ADMIN_EMAILS=admin@example.com
# אם יש כמה: ADMIN_EMAILS=admin1@example.com,admin2@example.com
ADMIN_EMAILS=admin@example.com


# ====================================
# SMTP - שליחת מיילים (חובה להתראות מנהל!)
# ====================================
# להשתמש ב-Hostinger Email:
# 1. התחבר לפאנל הניהול של Hostinger
# 2. עבור ל-Email Accounts ויצור חשבון מייל חדש
# 3. השתמש בפרטי החשבון שיצרת
# 4. וודא שכתובת המייל ב-EMAIL_FROM זהה ל-SMTP_USER

SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@fcmasterscup.com
SMTP_PASS=your-hostinger-email-password-here
EMAIL_FROM=noreply@fcmasterscup.com
SMTP_FROM=noreply@fcmasterscup.com

# בדיקה אם SMTP עובד:
# 1. הרץ את השרת - אמור לראות: "✅ SMTP verify OK"
# 2. גש ל-/api/admin/smtp/verify (כמנהל)
# 3. שלח טסט: POST /api/admin/smtp/test עם {"to":"your-email@example.com"}
# 4. בדוק לוגים: GET /api/admin/smtp/email-logs

# ====================================
# הגדרות אתר
# ====================================
# עבור פיתוח מקומי:
CORS_ORIGIN=http://localhost:5173
SITE_URL=http://localhost:5173

# עבור production (עם HTTPS + WebSocket Secure):
# CORS_ORIGIN=https://www.fcmasterscup.com
# SITE_URL=https://www.fcmasterscup.com
# 
# ⚠️ חשוב: אם האתר רץ על HTTPS, ה-WebSocket ישתמש ב-WSS (WebSocket Secure)
# ודא שה-Nginx מוגדר עם SSL Certificate (Let's Encrypt מומלץ)
# ראה הוראות ב: תיקון-WebSocket-SSL.md

# ====================================
# Presence System - Redis או זיכרון
# ====================================
# PRESENCE_DRIVER - בחר בין "redis" או "memory"
# memory = זמני, עובד בלי Redis (מומלץ עד שהתקנת Redis)
# redis = יציב עם TTL, דורש Redis מותקן ופעיל
PRESENCE_DRIVER=memory

# REDIS_URL - כתובת שרת Redis (אם משתמשים ב-redis driver)
REDIS_URL=redis://127.0.0.1:6379
# אם יש סיסמה: REDIS_URL=redis://:password@127.0.0.1:6379

# ====================================
# Tawk.to - צ'אט תמיכה (אופציונלי)
# ====================================
# להקים:
# 1. הירשם ב-https://www.tawk.to/
# 2. צור property חדש
# 3. העתק את ה-Property ID
# TAWK_PROPERTY_ID=your_tawk_property_id
# TAWK_WIDGET_ID=default

# ====================================
# הערות חשובות
# ====================================
# ⚠️ אל תעלה קובץ .env עם גיט!
# ⚠️ אל תשתף קובץ .env עם אף אחד (הוא ב-.gitignore)
# ⚠️ החלף את כל הערכים הדמה בערכים אמיתיים בפרודקשן!
# ⚠️ שנה ADMIN_EMAIL ו-ADMIN_PASSWORD בהרצה ראשונה בפרודקשן!
# ⚠️ הגדר JWT_SECRET חזק - זה קריטי לאבטחה!
